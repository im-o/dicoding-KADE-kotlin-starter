package learnclass

/**
 * created by Rivaldy on 11/1/2019
 */

fun main() {
    var a: String? = "stimed" // use ? for set null
    a = null

    //safe calss
    a?.length

    print(a?.length)
}